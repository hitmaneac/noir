/* Level 3 action handlers + world objects (noir office).
 * See lvl1/js/actions.js for the full API. Empty for now — paint yellow [I]
 * zones in the editor and map them here, or Nooir.addObject({...}) props. */
Nooir.actions.letter = function (z) {
  Nooir.playCharAnim("grab");
};
